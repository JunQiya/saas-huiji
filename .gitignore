# 后端构建产物
server/target/
*.class
*.jar

# 前端构建产物
admin/dist/
h5/dist/

# 前端依赖
admin/node_modules/
h5/node_modules/

# 日志与本地临时
*.log
.DS_Store
.idea/
.vscode/
.vite/

# 部署环境变量(含数据库密码/JWT密钥, 禁止入库)
.env
deploy/.env
# 但 .env.example 模板需入库(无敏感信息, 覆盖全局忽略)
!deploy/.env.example

# 内部测试脚本
.test-scripts/
